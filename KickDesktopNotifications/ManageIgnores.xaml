<Window x:Class="KickDesktopNotifications.ManageIgnores"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KickDesktopNotifications"
        xmlns:core="clr-namespace:KickDesktopNotifications.Core"
        mc:Ignorable="d"
        Title="Kick Notify: Manage Streams" Height="500" Width="600" ResizeMode="CanResizeWithGrip" Background="#0F0F0F" WindowStyle="None" AllowsTransparency="True">
    <Window.DataContext>
        <core:UIStreamer />
    </Window.DataContext>
    <Window.Resources>

        <SolidColorBrush x:Key="GotFocusColor" Color="#1A1A1A" />
        <SolidColorBrush x:Key="LostFocusColor" Color="#1A1A1A" />

        <Style TargetType="{x:Type DataGridColumnHeader}">
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="Background" Value="#1A1A1A"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="10,0,10,0"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="BorderBrush" Value="#53FC18"/>
            <Setter Property="BorderThickness" Value="0,0,0,2"/>
        </Style>
        <Style TargetType="{x:Type DataGridRow}">
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Background" Value="#0F0F0F"/>

            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#1A1A1A"/>
                </Trigger>

                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1A1A1A"/>
                </Trigger>

                <Trigger Property="ItemsControl.AlternationIndex" Value="0">
                    <Setter Property="Background" Value="#0F0F0F"/>
                </Trigger>

                <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                    <Setter Property="Background" Value="#141414"/>
                </Trigger>

                <EventTrigger RoutedEvent="DataGrid.GotFocus">
                    <BeginStoryboard>
                        <Storyboard>
                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Background">
                                <DiscreteObjectKeyFrame KeyTime="0:0:0" Value="{StaticResource GotFocusColor}" />
                            </ObjectAnimationUsingKeyFrames>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>

                <EventTrigger RoutedEvent="DataGrid.LostFocus">
                    <BeginStoryboard>
                        <Storyboard>
                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Background">
                                <DiscreteObjectKeyFrame KeyTime="0:0:0" Value="{StaticResource LostFocusColor}" />
                            </ObjectAnimationUsingKeyFrames>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Style.Triggers>
        </Style>
        
        <Style TargetType="{x:Type DataGridCell}">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,0,10,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type DataGridCell}">
                        <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="KickButton" TargetType="Button">
            <Setter Property="Background" Value="#53FC18"/>
            <Setter Property="Foreground" Value="#0F0F0F"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4" 
                                Padding="15,8,15,8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#6BFF3D"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="KickSecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#1A1A1A"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#53FC18"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4" 
                                Padding="15,8,15,8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#252525"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    
    <Border Background="#0F0F0F" BorderBrush="#53FC18" BorderThickness="2" CornerRadius="8">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="20" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="20" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="50" />
                <RowDefinition Height="60" />
                <RowDefinition Height="*" />
                <RowDefinition Height="20" />
                <RowDefinition Height="50" />
                <RowDefinition Height="20" />
            </Grid.RowDefinitions>
            
            <!-- Title Bar -->
            <Border Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="0" Background="#1A1A1A" CornerRadius="6,6,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <TextBlock Text="Kick Notify: Manage Streams" 
                               Foreground="#FFFFFF" 
                               FontSize="16" 
                               FontWeight="Bold" 
                               VerticalAlignment="Center" 
                               Margin="20,0,0,0"/>
                    <Button Content="✕" 
                            HorizontalAlignment="Right" 
                            VerticalAlignment="Center" 
                            Width="40" 
                            Height="40" 
                            Background="Transparent" 
                            Foreground="#FFFFFF" 
                            BorderThickness="0" 
                            FontSize="18" 
                            Click="CloseBtn_Click" 
                            Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="0,6,0,0">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#FF4444"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>
            
            <!-- Instructions -->
            <StackPanel Grid.Column="1" Grid.Row="1" VerticalAlignment="Center">
                <TextBlock TextWrapping="Wrap" 
                           Text="Add channels to monitor and toggle ignore to stop notifications." 
                           Foreground="#CCCCCC" 
                           FontSize="12" 
                           Margin="0,0,0,5"/>
                <TextBlock TextWrapping="Wrap" 
                           Text="Changes are automatically saved." 
                           Foreground="#999999" 
                           FontSize="11"/>
            </StackPanel>
            
            <!-- DataGrid -->
            <DataGrid 
                Grid.Column="1" 
                Grid.Row="2" 
                CanUserResizeColumns="False"
                x:Name="dgrdIgnore" 
                ItemsSource="{Binding StreamersToIgnore}" 
                AutoGenerateColumns="False" 
                CanUserAddRows="False" 
                RowHeaderWidth="0" 
                HeadersVisibility="Column" 
                Background="#0F0F0F" 
                BorderBrush="#53FC18"
                BorderThickness="1"
                GridLinesVisibility="None"
                AlternationCount="2">
                <DataGrid.Columns>
                    <DataGridCheckBoxColumn Header="Ignore" Binding="{Binding IsIgnored}" IsReadOnly="False" Width="80">
                    </DataGridCheckBoxColumn>
                    <DataGridTextColumn Header="Streamer Name" Binding="{Binding Name}" IsReadOnly="True" Width="*" />
                    <DataGridHyperlinkColumn Header="Streamer Link" Binding="{Binding Link}" Width="200">
                        <DataGridHyperlinkColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="#53FC18"/>
                                <EventSetter Event="Hyperlink.Click" Handler="HyperLink_Click"/>
                            </Style>
                        </DataGridHyperlinkColumn.ElementStyle>
                    </DataGridHyperlinkColumn>
                </DataGrid.Columns>
                <DataGrid.Resources>
                    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#53FC18"/>
                    <SolidColorBrush Color="#0F0F0F" x:Key="{x:Static SystemColors.HighlightTextBrushKey}" />
                </DataGrid.Resources>
            </DataGrid>
            
            <!-- Buttons -->
            <StackPanel Grid.Column="1" Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button Content="Add Channel" 
                        Width="120" 
                        Height="40" 
                        Margin="0,0,10,0"
                        Click="AddChannelBtn_Click" 
                        Style="{StaticResource KickButton}"/>
                <Button Content="Close" 
                        Width="100" 
                        Height="40" 
                        Click="CloseBtn_Click" 
                        Style="{StaticResource KickSecondaryButton}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
